name: 'Chromatic Deployment'
on: push

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - run: yarn
      - uses: chromaui/action@1
        with:
          projectToken: project-token
          token: ${{ secrets.CHROMATIC_TOKEN }}